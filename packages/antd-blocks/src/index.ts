/**
 * Main entry point for @brain-toolkit/antd-blocks
 *
 * This file exports all available modules.
 * Users can import from here for convenience, or use subpath imports for better tree-shaking.
 *
 * @example
 * // Full import
 * import { ResourceTable } from '@brain-toolkit/antd-blocks';
 *
 * @example
 * // Subpath import (recommended for better tree-shaking)
 * import { ResourceTable } from '@brain-toolkit/antd-blocks/resourceTable';
 * import { ResourceEvent } from '@brain-toolkit/antd-blocks/resourceTable/ResourceEvent';
 */

// Export ResourceTable module
export * from './resourceTable';
