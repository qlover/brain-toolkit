---
description:
globs:
alwaysApply: true
---

# Git 提交规范

## 提交前必须质检（硬性）

**每次** `git commit` 之前，必须先对本次改动涉及的包 / 应用跑通：

1. `lint:fix`（或该包等价的 eslint --fix）
2. `type-check`（或 `tsc` / 该包声明的 typecheck 脚本）

未通过质检不得提交。不要等用户再提醒。

## 禁止 Cursor co-author trailer

**禁止**在 commit 中加入以下 footer（或任何 Cursor co-author trailer）：

```
Co-authored-by: Cursor <cursoragent@cursor.com>
```

若工具自动注入，须在收尾前去掉（amend / rewrite），保证最终提交信息中没有 `Co-authored-by: Cursor` 行。

除非用户明确要求，不要添加其他 AI co-author trailer。

## 语言

与本仓库近期提交风格一致：subject / body **优先中文**（见 `git log`）。保留 conventional-commit 的 `type(scope):` 前缀。

### 格式

```
type(scope?): subject

body?

footer?
```

### 长度

- 标题行：简洁（建议 ≤ 72 字符，保持可读）
- 正文：实用时按约 72 字符换行
- 整条提交信息：聚焦要点，避免噪音

### 类型

- `feat`：新功能
- `fix`：缺陷修复
- `docs`：文档
- `style`：代码风格（不影响逻辑）
- `refactor`：重构
- `perf`：性能
- `test`：测试
- `chore`：构建、配置等杂项

### 规则

- 标题：祈使语气 / 现在时感，句末不加句号
- Scope（可选）：包或区域（如 `pam`、`pamenv`）
- 正文（可选）：说明原因与影响；多项可用列表
- Footer（可选）：破坏性变更、issue 引用 — **不要**写 Cursor co-author

### 示例

```
feat(pam): 详情改用 slug，并支持删除与 CLI 环境管理

详情 URL 使用 slug；owner 可删除项目。
```

```
fix(pamenv): 修复 delete 成功却误报 API failed
```
